# =============================================================================
# INCUBAZAR - Trust-First Digital Incubator
# Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!

# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Get these from your Supabase project settings: https://app.supabase.com
# Project Settings > API > Project URL and API Keys
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# =============================================================================
# APPLICATION CONFIGURATION (REQUIRED)
# =============================================================================
# Base URL of your application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment (development, production, test)
NODE_ENV=development

# =============================================================================
# AUTHENTICATION (REQUIRED)
# =============================================================================
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_key_here

# =============================================================================
# RAZORPAY PAYMENT GATEWAY (REQUIRED FOR SUBSCRIPTIONS)
# =============================================================================
# Get credentials from: https://dashboard.razorpay.com/app/keys
# Used for founder subscription payments
NEXT_PUBLIC_RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# =============================================================================
# DOCUSIGN E-SIGNATURE (REQUIRED FOR DOCUMENT SIGNING)
# =============================================================================
# Get credentials from: https://developers.docusign.com/
# Used for Term Sheets and PAS-4 document signing
DOCUSIGN_INTEGRATION_KEY=your_docusign_integration_key
DOCUSIGN_USER_ID=your_docusign_user_id
DOCUSIGN_ACCOUNT_ID=your_docusign_account_id
DOCUSIGN_PRIVATE_KEY=your_docusign_private_key_base64_encoded

# DocuSign URLs (Use demo for development, production for live)
DOCUSIGN_OAUTH_BASE_URL=https://account-d.docusign.com
DOCUSIGN_API_BASE_URL=https://demo.docusign.net/restapi

# =============================================================================
# MATCHING ALGORITHM
# =============================================================================
# Smart matching is built-in using TypeScript!
# No external AI service needed - everything runs on Next.js
# Matching algorithm: /lib/matching/algorithm.ts
# API endpoints:
#   - /api/matching/recommendations (for investors)
#   - /api/matching/investors (for founders)

# =============================================================================
# SUPABASE STORAGE BUCKETS
# =============================================================================
# Create these buckets in your Supabase project:
# Storage > Create new bucket
# 
# Required buckets:
# - documents (for KYC documents, pitch decks) - Private
# - logos (for startup logos) - Public
# - generated-docs (for auto-generated legal documents) - Private
#
# No environment variables needed, just ensure buckets exist

# =============================================================================
# OPTIONAL INTEGRATIONS
# =============================================================================

# OpenAI API (For enhanced AI features - optional)
# OPENAI_API_KEY=your_openai_api_key

# Sentry Error Tracking (For production monitoring - optional)
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Google Analytics (For traffic analytics - optional)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Resend Email Service (For transactional emails - optional)
# RESEND_API_KEY=your_resend_api_key

# =============================================================================
# COMPLIANCE & SECURITY
# =============================================================================
# Maximum investors per deal (Section 42 compliance)
# Default: 200 (DO NOT CHANGE unless you know what you're doing)
NEXT_PUBLIC_MAX_INVESTORS_PER_DEAL=200

# Rate limiting configuration (requests per minute)
RATE_LIMIT_STANDARD=60
RATE_LIMIT_STRICT=10
RATE_LIMIT_AUTH=5

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel Deployment:
# 1. Go to your project settings on Vercel
# 2. Navigate to Environment Variables
# 3. Add all REQUIRED variables above
# 4. Redeploy your application
#
# For Production:
# 1. Change NODE_ENV to 'production'
# 2. Update NEXT_PUBLIC_APP_URL to your production URL
# 3. Use production DocuSign URLs
# 4. Enable and configure Sentry for error tracking
# 5. Set up proper CORS policies in Supabase
#
# Database Setup:
# 1. Run migrations in order: 
#    - 001_initial_schema.sql
#    - 002_rls_policies.sql
#    - 003_fix_rls_policies.sql (if exists)
#    - 004_investor_limit_trigger.sql
#    - 005_startup_details.sql (comprehensive startup data)
# 2. Create storage buckets as listed above
# 3. Configure RLS policies on storage buckets
#
# =============================================================================
